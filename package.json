{
  "name": "dressvintage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DressVintage — frontend estático + serverless functions (newsletter, orders, emails).",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "preview-emails": "node scripts/preview-emails.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "resend": "^4.0.0"
  }
}
